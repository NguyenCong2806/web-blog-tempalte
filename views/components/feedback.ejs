<section class="py-12 sm:py-16 md:py-20 lg:py-28 bg-teal-50 relative overflow-hidden">
  
  <div class="absolute top-0 left-0 w-48 h-48 sm:w-64 sm:h-64 bg-teal-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob"></div>
  <div class="absolute top-0 right-0 w-48 h-48 sm:w-64 sm:h-64 bg-blue-100 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-blob animation-delay-2000"></div>

  <div class="container mx-auto px-4 sm:px-6 relative z-10">
    
    <div class="text-center mb-12 sm:mb-14 md:mb-16 lg:mb-20" data-aos="fade-up">
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-teal-900 uppercase tracking-wide">
        Cảm nhận Học viên
      </h2>
      <div class="mt-3 h-1.5 w-20 sm:w-24 bg-orange-400 mx-auto rounded-full"></div>
      <p class="mt-4 sm:mt-5 text-sm sm:text-base text-gray-600 max-w-2xl mx-auto px-2">
        Lắng nghe những chia sẻ thực tế từ các cựu sinh viên và đối tác doanh nghiệp.
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6 md:gap-8">
      
      <% var items = feedbacks %>

      <% items.forEach(function(item) { %>
        
        <div data-aos="fade-down-right" class="bg-white rounded-2xl p-6 sm:p-7 md:p-8 shadow-sm hover:shadow-lg transition-all duration-300 transform hover:-translate-y-1 border border-gray-100 flex flex-col h-full relative group">
          
          <div class="absolute top-4 sm:top-6 right-6 sm:right-8 text-5xl sm:text-6xl text-teal-100 font-serif opacity-50 group-hover:text-teal-200 transition-colors pointer-events-none select-none">
            ”
          </div>

          <div class="w-10 h-10 sm:w-12 sm:h-12 mb-4 sm:mb-6 rounded-full bg-teal-50 flex items-center justify-center text-teal-600 shrink-0">
            <i class="fa-solid fa-quote-right text-lg sm:text-xl"></i>
          </div>

          <div class="relative z-10 mb-5 sm:mb-6 flex-grow">
            <div class="text-gray-600 italic leading-relaxed text-sm sm:text-base prose prose-sm max-w-none line-clamp-4">
               <%- item.detail %>
            </div>
          </div>

          <div class="w-full h-px bg-gray-100 mb-5 sm:mb-6"></div>

          <div class="flex items-center gap-3 sm:gap-4 relative z-10">
            
            <div style="width: 4rem; height: 4rem; margin-right: 20px;" class="rounded-full p-1 bg-white border border-gray-200 shadow-md flex-shrink-0 mr-7">
              <img 
                src="<%= item.img || 'https://placehold.co/100x100?text=U' %>" 
                alt="<%= item.title %>" 
                class="w-full h-full object-cover rounded-full" 
                loading="lazy"
                onerror="this.src='https://placehold.co/100x100?text=User'"
              >
            </div>

            <div class="flex flex-col gap-0.5 sm:gap-1 flex-1 min-w-0">
              <h4 class="font-bold text-gray-900 text-sm sm:text-base group-hover:text-teal-700 transition-colors truncate">
                <%= item.title %>
              </h4>
              
              <% if (item.job) { %>
                <span class="text-xs text-gray-500 font-medium line-clamp-1">
                  <%= item.job %>
                </span>
              <% } %>
            </div>

          </div>

        </div>

      <% }); %>

    </div>
  </div>
</section>