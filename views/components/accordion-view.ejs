<div class="container mx-auto px-4">
  <div class="grid grid-cols-1 lg:grid-cols-12 gap-6 items-start">
    <!-- Accordion column (left on large screens) -->
    <div class="lg:col-span-7">
      <div class="">
        <% const items = typeof accordions !== 'undefined' && Array.isArray(accordions) ? accordions : [
          { title: 'Sở hữu Bằng Đại học uy tín', body: 'Hệ thống đào tạo đảm bảo cấp bằng chính quy hợp pháp và có giá trị trên toàn quốc.', open: true, icon: '🎓' },
          { title: 'Thời gian đào tạo', body: 'Thời gian đào tạo linh hoạt theo từng chuyên ngành và chương trình học.', icon: '📅' },
          { title: 'Cố vấn học tập', body: 'Cố vấn học tập đồng hành trong suốt quá trình học để hỗ trợ sinh viên.', icon: '🚚' },
        ]; %>

        <div class="space-y-4">
        <% items.forEach(function(item, idx) { %>
          <details class="group" <%= item.open ? 'open' : '' %>>
            <summary class="flex items-center justify-between gap-4 cursor-pointer rounded-lg bg-teal-50 hover:bg-teal-100 py-4 px-5 shadow-sm">
              <div class="flex items-center gap-4">
                <div class="flex items-center justify-center w-10 h-10 rounded-md bg-white text-teal-700 text-lg shadow-sm">
                  <% if (item.iconHtml) { %>
                    <%- item.iconHtml %>
                  <% } else if (item.icon) { %>
                    <span aria-hidden="true"><%= item.icon %></span>
                  <% } else { %>
                    <!-- default icon -->
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-teal-700" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 2l3 7h7l-5.5 4 2 7L12 16l-6.5 4 2-7L2 9h7z" />
                    </svg>
                  <% } %>
                </div>
                <div class="text-left">
                  <div class="text-base md:text-lg font-semibold text-teal-800"><%= item.title %></div>
                </div>
              </div>

              <div class="flex-shrink-0">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-red-600 transform transition-transform duration-200 group-open:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
              </div>
            </summary>

            <div class="mt-2 px-5 pb-4 text-sm sm:text-base text-teal-700 bg-white rounded-b-lg">
              <%= item.body %>
            </div>
          </details>
        <% }); %>
        </div>
      </div>
    </div>

    <!-- Image column (right on large screens) -->
    <div class="lg:col-span-5">
      <div class="w-full h-56 sm:h-64 md:h-80 lg:h-full bg-gray-50 rounded-lg flex items-center justify-center overflow-hidden">
        <img src="/image/mau-bang-dai-hoc-tu-xa-tnu-ngon-ngu-anh-1076x800.jpg" alt="Phôi bằng Đại học Thái Nguyên"
          class="w-full h-full object-contain md:object-cover" />
      </div>
    </div>
  </div>

</div>