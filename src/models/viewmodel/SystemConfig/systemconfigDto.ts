
class UIConfig {
  baseFontSize: string;
  lineHeight: string;
  primaryColor: string;
}

class SEOConfig {
  googleAnalyticsId: string;
  googleConsoleVerification: string;
  headerScript: string;
  footerScript: string;
}

export class SystemConfig {
  uiConfig: UIConfig;
  seoConfig: SEOConfig;
  websiteName: string;
  contactEmail: string;
  hotline: string;
}